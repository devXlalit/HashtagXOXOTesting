import React from "react";

const Title = ({ title }) => {
  return (
    <div className="text-center px-16 text-2xl md:text-5xl font-medium text-[#343a40]">
      {title}
    </div>
  );
};

export default Title;
