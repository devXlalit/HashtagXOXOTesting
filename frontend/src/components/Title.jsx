import React from "react";

const Title = ({ title }) => {
  return (
    <div className="text-center text-3xl font-medium text-[#9A3B3B]">
      {title}
    </div>
  );
};

export default Title;
